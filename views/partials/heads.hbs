<!DOCTYPE html>
<html lang='en'>
    <head>
        <title>Chain Dating!</title>
        <meta charset='UTF-8'>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel='stylesheet' href='/css/bootstrap.min.css'>
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">ChainDating</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link{{#if isMainPage }} active{{/if}}" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{#if isDatingPage }} active{{/if}}" href="/dating">Find!</a>
                    </li>
                    {{#if isSignedIn}}
                    <li class="nav-item">
                        <a class="nav-link{{#if isOptionsPage }} active{{/if}}" href="/options">Settings</a>
                    </li>
                    {{/if}}
                    <li class="nav-item">
                        <a class="nav-link{{#if isTradePage }} active{{/if}}" href="/trade">Token</a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    {{#unless isSignedIn}}
                    <li class="nav-item">
                        <a class="nav-link{{#if isSignInPage }} active{{/if}}" href="/sign-in">Sign In</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{#if isRegisterPage }} active{{/if}}" href="/register">Register</a>
                    </li>
                    {{/unless}}
                    {{#if isSignedIn}}
                    <li class="nav-item">
                        <a class="nav-link{{#if isSignOutPage }} active{{/if}}" href="/sign-out">Sign Out</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>